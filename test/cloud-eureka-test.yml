server:
  port: 9020
spring:
  application:
    name: cloud-eureka
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:9020/eureka